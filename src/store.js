import { reactive } from "vue";
// import productsJSON from '../db.json'; - import from db.json

export const store = reactive({
    // products: productsJSON.products - import from db.json
    products: [],
})